import React from 'react'
import { Button, Header, Icon, Segment } from 'semantic-ui-react'

const ParkingLocationData = () => (
  <Segment placeholder>
    <Header icon>
      <Icon name='map marker alternate' />
      Parking Location
    </Header>
    
  </Segment>
)

export default ParkingLocationData;